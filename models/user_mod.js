const user_model = ()=> {

    let User = {
        first_name: firstName,
        last_name: lastName,
        email: email,
        password_hash:passWord,
        role:role
    }
    return User
} 

module.exports = {
    user_model
}